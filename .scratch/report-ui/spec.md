# 报表 UI 批次（report-ui）spec

> Status: 已实现（2026-09-22，R1~R8 落地 + 用户验收后 6 条修正，见 Comments）
> 决策时间：2026-09-22，需求来自用户六条想法，逐条已确认
> 实现顺序建议：R2 → R4 → R3 → R6 → R1 → R5 → R7（先删后加，纯前端收尾）

## 背景

当前报表（`templates/` + `src/report.py` + `templates/static/app.js|css`）只有折扣降序一种排序，
卡片详情排版松散，链接无图标。用户提出 6 条改进，本文档是定稿。

## 需求（用户确认版）

### R1 排序（组内）

- 维度三选一：**默认 = 折扣降序（现状）** / 价格升 / 好评率降。
- 好评率排序附带「好评数量范围筛选」chips：全部 / ≥500 / ≥5000，
  作用于**当前组**的显示集合；「详情待补」（无 reviews）的条目不参与好评率排序，固定排组尾。
- 排序范围：**只影响当前分组**，不跨组。
- 切换排序或筛选后，该组回到第 1 页。
- payload 需补数值字段 `price_int`（`reviews.score/count` 已在 payload）；
  前端不用 `price_text` 字符串排序。

### R2 删除 ITAD 链接

- 用户实测：`itad_url`（ITAD deal 链接）302 直跳 Steam 商店页，信息冗余 → **删除**。
- 卡片展开态去掉 ITAD 链接（`app.js buildCard` 的第三个链接）。
- `classify.SEEN_KEEP` 去掉 `itad_url`（新落库省 ~348KB/全量）；
  `classify.py` 里「itad_url 348KB 可砍」的旧注释更新为本决策。
- 旧 `state.json` 里已落的 `itad_url` 字段**留存不迁移**（下次该条目更新时自然消失）。

### R3 详情排布

- `card-detail` 中间加**竖向分隔线**，分左右两栏：
  - 左栏 = 价格类：Steam 史低 / 全周期最低 / 近一年最低；
  - 右栏 = 时间 + 比价：折扣开始 / 折扣结束 / 各比价行。
- 窄屏（≤ 断点 768px）回落单栏纵向堆叠。

### R4 链接前置 + SVG 图标

- Steam / 小黑盒链接移到 `card-summary` 的**价格区、折扣价之前**（横向小图标排布）。
- 图标用 **inline SVG 常量**内嵌 `app.js`（不下载 favicon 文件、不发外链请求）；
  链接保留文字 `aria-label`。ITAD 不再有链接（R2）。

### R5 密度切换（一行一条 / 一行两条）

- 页面级控件：**单列 / 双列**。手机端默认单列；PC 端可切双列（每行两张卡）。
- 选择存 `localStorage`，刷新保持。
- 双列时**每页条数不变**（不与断点分页逻辑耦合，避免「布局与每页条数错位」的老坑 §7.3）。

### R6 英文名显示

- payload 里 `title`（ITAD 英文名）已有，**零新增请求**。
- 规则：**PC + 单列** → 卡片标题旁灰色小字英文；
  **手机端 / 双列模式** → 只在详情展开里显示「原名」一行。
- fallback：若联动实现麻烦，全部放详情展开里（用户已许可）。

### R7 方向键翻页

- `←` / `→` 对「当前活动分组」翻页。
- 活动分组 = 最近一次被点击翻页按钮 / 卡片的分组；无记录时 = 第一个未折叠分组。
- 与现有按钮翻页共用 `pages` 状态；输入焦点在输入控件上时不响应（当前页面无输入框，防御性判断即可）。

### R8 封面图换小尺寸

- 现状：卡片缩略图用 `banner600`（回落 `banner400`/`boxart`），但缩略图只有几十像素宽，
  600px 大图加载慢（用户反馈）。
- 改法：`report.py` 的 payload `banner` 字段改为**优先 `boxart`**（ITAD assets 里的小尺寸），
  `banner600/400` 不再使用；`boxart` 缺失时保持现状回落（前端已有无图模式兜底）。

## 验收

- [x] `python tools/check_payload.py` 通过（分组标签一致性 / 中文名 / 比价覆盖率；check_payload 另加了「无 itad_url」判定）。
- [x] payload 与卡片中**无 `itad_url`**；`SEEN_KEEP` 不再含该字段（单测）。
- [x] 排序三维度 + 好评数筛选在单组内生效，切换后回第 1 页（浏览器手测：用户验收）。
- [x] 768px 断点两侧：详情两栏/单栏、双列/单列、链接图标位置均正确（密度切换已按 Comments 取消）。
- [x] 英文名按 Comments 修正后的规则显示（始终在中文标题下方，固定占位）。
- [x] `←`/`→` 翻页与按钮一致（浏览器手测：用户验收）。

## Comments

### 实现记录（2026-09-22）

- R2/R8 在 payload 侧：`classify.SEEN_KEEP` 去 `itad_url`（normalize_item 不再产出）、
  `report.build_card` 去 `itad_url`、加 `price_int`、`banner` 优先 boxart。
  旧 state.json 里已落的 itad_url 留存不迁移，下次该条目更新时自然消失。
- R1/R3/R4/R6/R7 在 `app.js`：组内排序工具条（每组独立状态）、详情左右两栏 + 竖分隔线、
  inline SVG 链接前置、英文名、方向键翻页（活动分组 = 最近点击的翻页按钮/卡片，
  无记录回落第一个未折叠分组）。
- 验收：95 个单测全过；`check_payload.py` 判定全 ✓（分组标签一致 / 无 itad_url /
  中文名 142/142 / 比价 142/142，以正式 run.py 产物为准）。

### 用户验收后的修正（2026-09-22 晚，覆盖上文部分需求）

1. 封面改竖框（boxart 是 2:3 竖图）：PC 44×66、手机 40×60，不再裁切；
   英文名**始终**显示在中文标题下方（本条覆盖 R6 的「手机/双列放详情」规则与
   「详情两栏里加原名行」的想法）。
2. 比价行改为 `₴45 ≈ ¥6.75 -30%`：±百分比带色（便宜绿 / 贵红 / 同价灰），
   payload 用数值 `diff_pct` 替代 `diff_text` 文案。
3. **R5 密度切换取消**（覆盖 R5）：不做单/双列切换与 localStorage，CSS 默认一行两卡，
   ≤768px 收单列 —— 断点与分页断点一致（§7.3 的坑不再出现）。
4. 新增组内「仅新史低」chip（用户新增需求）：作用于当前组显示集合，只留 flag=N，
   切换后回第 1 页。
5. 概览四个统计框压矮、数值+文字横排；顺序改为 进列表 / 当日新增 / 史低总数 / 抓取总数；
   手机两排四框。
6. 英文名行固定占位高度（空也渲染），有/没有中文名的卡片在双列网格里对齐。

### 测试工具

- `tools/render_report.py`：全本地零网络请求的测试渲染（不抓 ITAD、不打 Steam、
  不写 state）；当日候选从 seen_deal 重建，比价行从上一次 output/data.js 按 appid 回收，
  汇率只读当天缓存。产出的只是测试报表，正式报表仍由 run.py 产出。
- Steam 侧请求额度宝贵（store host 有静默超时惩罚，见交接文档 §3），
  UI 迭代一律用这个脚本，别为看版式重跑全流程。

### 批 C（2026-09-23，用户反馈五条，与 §3.6 storelow 同批落地）

1. **手风琴展开**：同时最多展开一张卡片 —— 点开新卡先收起所有 `.card.open` 再开本卡（app.js buildCard）。
2. **现价改促销红并加大加粗**：新增 `--price-now: #d92b2b`；`.price-now` 17px/700（用户反馈绿色不显眼）。
   ⚠️ 只动现价：比价行的「便宜绿/贵红」（diff-cheap/diff-dear）是上一批验收过的语义色，保持不动；
   折扣 % tag（tag-cut）也保持绿，避免与新史低红标混淆。
3. **小黑盒图标**：原为手绘「机器人脸」与真 logo 不符（非链接/下载问题——R4 不下载 favicon）。
   **用户已自行逐像素重绘**（官方 PNG→251px 实测顶点、两块 180° 对称 Z 形片、IoU 0.956；
   追踪脚本 tools/heihe_*.py 属一次性工具未入库）。
4. **链接图标对齐**：图标不再与折扣价格同行对齐（价格位数不一挤不出固定空位），
   改 `align-self: flex-end` 沉到摘要底部、与「新史低/折扣%」tags 行底部对齐 → 跨卡横向对齐。
5. **上次史低时间（§3.6 storelow/v2）**：用户确认显示规则 —— `N` 显示「本次刷新历史记录」、
   `H/S` 显示「X 天前（YYYY-MM-DD）」，左栏价格类补行。实现见 DEVELOPMENT.md §3.6（数据侧）
   与 run.py/report.py；卡片经 payload `last_low_text` 渲染，取不到不渲染该行。
- 验收方式：本地 state 从 data 分支同步（`git show origin/data:data/state.json > data/state.json`）后
  跑 `tools/render_report.py`，浏览器打开 output/index.html 检查。
  ⚠️ 当日新增里 `last_low_at` 要等下次 Actions 主跑（含 storelow 步骤）才有数据，
  本地预览时 H/S 行可能暂缺，属预期。

### 批 C2（2026-09-23，预览后追加四条）

1. **链接图标挪进档位标签行**：Steam/小黑盒图标放「好评达标」等档位标签**右侧同一行内**
   （.tags 内 append）——同行天然对齐，替代批 C 的「沉底对齐」方案；iconLink 加
   stopPropagation，点图标开新标签页不触发展开/收起。
2. **无封面图保留占位空块**：`div.thumb.thumb-empty`（.thumb 自带浅灰底），双列网格不错位。
3. **上次史低固定在左栏最末行**：三个价格（Steam 史低/全周期最低/近一年最低）下方。
4. **档位「好评达标」标签改浅蓝**：Steam 品牌好评色 `#66c0f4` 专为深底设计，白底 11px
   对比度 ~1.9:1 不可读 → 压深一档 `#1f96d2` + 底 `#e6f5fd`（tag-quality）。

### 代码审查记录（2026-09-22，双轴，针对 5dbc811 快照）

- **已修**：app.js 文件头注释仍写「R5 密度切换」，与 Comments#3 的取消决定矛盾 → 已更正。
- 记录不阻塞（judgement call）：CSS 媒体查询硬编码 768px 与 JS 可配置 breakpoint 仅靠约定一致
  （改 `mobile_breakpoint_px` 会复活 §7.3 错位，若改配置需同步 CSS）；
  `build_card` 的 `or entry.get("banner")` 回落是死代码（SEEN_KEEP 从未落 banner，保留无害）；
  render_report.py 手工解析 data.js 的形状重复；方向键对已折叠的活动组仍翻页、
  点卡片外链会改写活动分组（spec 未定义，轻微）。
