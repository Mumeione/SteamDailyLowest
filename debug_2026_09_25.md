---
title: 检查报告-2026-09-25
---

# SteamDailyLowest 检查报告

**检查日期**：2026-09-25  
**检查对象**：https://github.com/Mumeione/SteamDailyLowest  
**线上报表**：https://mumeione.github.io/SteamDailyLowest/  
**线上产物版本**：`生成时间 2026-09-25 07:52`，`assets_version=1790293934`  
**对应运行**：daily #8（commit `3b72077`，排队 28 分钟后于北京时间 07:52 启动）

---

## 一、结论摘要

本次检查围绕三个问题展开，结论如下：

| # | 问题                       | 结论                                                                                                                           | 严重度    |
| - | ------------------------ | ---------------------------------------------------------------------------------------------------------------------------- | ------ |
| 1 | 热门游戏中文名大量缺失              | **确认为 Bug**。Steam `appdetails` 会把请求的 appid 重定向到新 appid，代码只按请求 appid 取结果，导致必然取不到中文名。抽样 12 个 `title_zh=null` 的条目，**重定向率 100%** | **P0** |
| 2 | 卡片详情区应为两栏、实际显示三栏         | **确认为 Bug**。`quality` 组内有 1 张卡折扣开始时间差 1 分钟，导致组级 `start_text` 被置 `null`，121 张卡的"折扣开始"行无法上提到组头，每张卡多渲染一行                        | **P1** |
| 3 | 现价显示 `¥12.7` 而非 `¥12.70` | **确认为 Bug**。`format_amount` 用 `rstrip("0")` 砍尾零，破坏金额两位小数惯例                                                                   | **P1** |
| 4 | Action 排队时长              | 正常。当日排队 28 分钟                                                                                                                | P2     |

**核心发现**：问题 1 推翻了此前"Steam 上本就没有中文标题、属正常回落"的判断。实测证明**大量热门游戏在 Steam 上确有官方中文名，只是被 appid 重定向机制吞掉了**。

---

## 二、问题 1（P0）：中文名被 appid 重定向吞掉

### 2.1 复现证据

对用户指定的 5 款热门游戏，逐一请求 Steam `appdetails`（`l=schinese&cc=cn`），实测结果：

| 游戏                           | 请求 appid | Steam 返回 key | 返回的 name                      | 是否重定向 | 有中文？   |
| ---------------------------- | -------- | ------------ | ----------------------------- | ----- | ------ |
| Metro Exodus                 | 412020   | **1952352**  | **地铁：离乡**                     | 是     | ✅ 有    |
| Persona 3 Reload             | 2161700  | **2334536**  | **女神异闻录３ Reload**             | 是     | ✅ 有    |
| Kingdom Come: Deliverance II | 1771300  | **3677330**  | **天国：拯救2**                    | 是     | ✅ 有    |
| Metaphor: ReFantazio         | 2679460  | **2924480**  | **暗喻幻想：ReFantazio**           | 是     | ✅ 有    |
| Borderlands: The Pre-Sequel  | 261640   | 321430       | `Borderlands: The Pre-Sequel` | 是     | ❌ 真无中文 |

**5 款里 4 款有官方中文名**，但线上 `data.js` 显示这 5 款的 `title_zh` **全部为 `null`**。

### 2.2 扩大抽样

从线上 `data.js` 中抽取 12 个 `title_zh=null` 且有 appid 的条目，逐个单请求验证：

| 请求 appid | 返回 key  | 重定向 | 返回的 name                               |
| -------- | ------- | --- | -------------------------------------- |
| 223100   | 500272  | ★   | Homefront®: The Revolution             |
| 597820   | 1549560 | ★   | BIOMUTANT                              |
| 1501750  | 2387880 | ★   | **Lords of the Fallen 堕落之主**           |
| 1451190  | 2946350 | ★   | Undisputed                             |
| 2806050  | 4485640 | ★   | Halo: Campaign Evolved                 |
| 3282300  | 4572310 | ★   | **雾影猎人**                               |
| 261640   | 321430  | ★   | Borderlands: The Pre-Sequel            |
| 1153640  | 2237300 | ★   | **Chorus**                             |
| 678960   | 1196141 | ★   | **CODE VEIN**                          |
| 803330   | 1621710 | ★   | Destroy All Humans!                    |
| 921800   | 1133130 | ★   | Encased: A Sci-Fi Post-Apocalyptic RPG |
| 1084160  | 2469350 | ★   | Jagged Alliance 3                      |

**12/12 全部发生重定向，重定向率 100%。**

### 2.3 根因定位

问题出在 `src/steam.py`：

```python
def _appdetails(self, appids: list[int], cc: str, filters: str) -> dict[int, dict]:
    """调一次 appdetails，返回 {appid: data}（拿不到的不在字典里）。"""
    params = {
        "appids": ",".join(str(a) for a in appids),
        ...
    }
    data = self.request("GET", "/api/appdetails", params=params)
    ...
    for key, entry in data.items():
        ...
        out[int(key)] = payload        # ← key 是 Steam 返回的 key，可能是重定向后的新 appid
    return out

def info(self, appid: int, cc: str = "CN") -> dict | None:
    data = self._appdetails([appid], cc, SINGLE_FILTERS)
    payload = data.get(appid)          # ← BUG：用「请求的 appid」去查，而字典 key 是「返回的 appid」
    if not payload:
        return None                    # ← 取不到就返回 None，中文名静默丢失
```

**判定链**：

```
请求 appdetails(appids=412020)
    ↓
Steam 返回 {"1952352": {"success": true, "data": {"name": "地铁：离乡", ...}}}
    ↓
_appdetails() 建字典 {1952352: {...}}
    ↓
info() 执行 data.get(412020)  →  None
    ↓
return None
    ↓
enrich.py: if not info or not info.get("name"): continue  →  中文名静默丢弃
```

### 2.4 为什么之前被误判为"正常"

`src/steam.py` 的模块 docstring 里写着：

> `name` 是否中文只取决于 `l=schinese` 与「Steam 上这个游戏**有没有**中文标题」—— 没有中文标题的游戏返回英文名，属正常，不能当失败。

这句话本身没错，但**掩盖了重定向 bug** —— 因为 bug 的表现（拿不到名字）和"Steam 真没中文名"的表现（返回英文名）在最终结果上都是"卡片显示英文名"，从外部无法区分。全库统计 `2423/4227 = 57.3%` 无中文名，此前被归因为"数据源特性"。

### 2.5 影响面

| 指标               | 数值              |
| ---------------- | --------------- |
| `game_meta` 总量   | 4227            |
| 有 `title_zh`     | 1804（42.7%）     |
| 无 `title_zh`     | 2423（**57.3%**） |
| 其中「有 appid 且已抓过」 | 2368            |

今日 daily 的 `title_zh_fetched=41 / title_zh_cached=0`，即在 127 款进列表游戏里只成功抓到 41 个中文名。**这 41 个恰好是"返回 key 与请求 key 相同"的那批**。

### 2.6 解决方案

#### 方案 A（推荐）：接受任意 key，改用返回值反查

因为 `info()` 是**单 appid 请求**，响应里必然只有一个条目，无需用 appid 匹配：

```python
def info(self, appid: int, cc: str = "CN") -> dict | None:
    data = self._appdetails([appid], cc, SINGLE_FILTERS)
    if not data:
        return None
    # 单 appid 请求：Steam 可能重定向，返回 key 未必等于请求 appid。
    # 响应只含一个条目，直接取第一个即可。
    payload = next(iter(data.values()))
    ...
```

**优点**：改动最小（3 行），语义正确，覆盖所有单请求场景。  
**风险**：若 Steam 某次返回多条（不应发生），取第一条可能不精确 —— 可加断言防御。

#### 方案 B：同时支持匹配请求 key 与返回 key

```python
payload = data.get(appid)
if payload is None and len(data) == 1:
    payload = next(iter(data.values()))          # 重定向场景
if payload is None:
    # 兜底：按名字模糊匹配（不推荐，仅作最后防线）
    return None
```

**优点**：对正常与重定向两种情况都显式处理，可读性好。  
**缺点**：比 A 略啰嗦。

#### 方案 C：用 `api/appdetails` 之外的端点绕开

例如先调 `storesearch` / `api/apps/{id}` 获取规范 appid 再查。**不推荐** —— 多一次请求，且 Steam 规范 appid 本身也会变，等于把问题推给另一个接口。

#### 方案 D：改用 Steam 商店页面抓取

直接请求 `store.steampowered.com/app/{appid}/?l=schinese` 解析 `<title>` 或 `og:title`。

**优点**：不受 API 重定向影响（页面响应会跟随重定向并返回最终页内容）。  
**缺点**：要写 HTML 解析、页面结构变更风险高、请求更重。仅在前述方案都失效时考虑。

**建议**：采用 **方案 A**，并在 `_appdetails` 的 docstring 中补充"key 可能是重定向后的 appid"这一实测结论，避免后人再踩。

### 2.7 修复后需处理的历史数据

`state.json` 中 2368 条"有 appid 但无 title_zh"的记录，`title_zh_at` 字段为空，说明**从未成功写入过**。修复后这些条目的 `state.title_zh(game_id)` 仍返回 `None`，会自然进入重抓路径 —— 但受 `prefetch_daily_budget=300` 限制，**需 8 天左右才能补齐**（2368 / 300 ≈ 8）。

可选：写一次性脚本，用 `game_meta.appid` 直接批量重抓中文名，不受每日预算限制。

> ⚠️ 注意：`title_zh` 是**永久缓存**，而 bug 期间**没有写入任何错误值**（`set_title_zh` 只在拿到 name 时调用），所以**不存在脏数据需要清理**。修复后重抓即可填补。

---

## 三、问题 2（P1）：详情区多出一行导致"三栏"

### 3.1 现象

用户反馈：卡片详情区应为**两栏**，线上实际呈现**三栏**。

### 3.2 实测

线上 `data.js` 中，`quality` 组（121 张卡）的组级元信息：

```json
{"key": "quality", "label": "好评达标", "criteria": "好评率 ≥ 70% 且 评价数 ≥ 100",
 "collapsed": false, "count": 121, "start_text": null}
```

而 `notable` 组：

```json
{"key": "notable", ..., "collapsed": true, "count": 6, "start_text": "2026-09-25 01:20"}
```

`quality` 组 `start_text` 为 `null`，组内卡片 `start_text` 分布：

```
"2026-09-25 01:20"  →  73 张
"2026-09-25 01:21"  →   1 张   ← 唯一异类
```

### 3.3 根因

`src/report.py::build_groups`：

```python
starts = {item["start_text"] for item in group_items if item.get("start_text")}
group_start = starts.pop() if len(starts) == 1 else None
```

组内出现 2 种不同值 → 集合 size 为 2 → `group_start = None`。

前端 `templates/static/app.js`：

```js
if (!(group && group.start_text)) {
  var startRow = detailRow("折扣开始", item.start_text);
  if (startRow) leftCol.insertBefore(startRow, leftCol.firstChild);
}
```

组级 `start_text` 为空 → 进入分支 → **每张卡左栏都插入一行"折扣开始"**：

```
设计（2 行）：          实际（3 行）：
距上次史低  │ 乌克兰区    折扣开始   ← 多出
折扣结束    │ 印度区      距上次史低  │ 乌克兰区
                        折扣结束    │ 印度区
```

### 3.4 为何"所有卡片都是三栏"

`quality` 组默认展开（`collapsed: false`），121 张卡全部走逐卡渲染分支。`notable` 组虽正常（2 行），但 `collapsed: true` 默认收起。**用户肉眼可见的展开卡片全部来自 `quality` 组，因此看起来"所有卡片都是三栏"。**

### 3.5 说明：DOM 层仍是两栏

实测 DOM 几何（本地渲染复现）：

```
DIV.detail-cols @(216,369) 409x48     ← flex，2 个子元素
  DIV.detail-col @(216,369) 194x48    ← 左栏
  DIV.detail-col @(420,369) 205x48    ← 右栏
```

视觉上的"三栏"是**右栏内部 `space-between` 撑开的空隙** + **左栏多一行**共同造成的误读。但根本问题是**本应上提到组头的行没有上提**，属真实缺陷。

### 3.6 解决方案

#### 方案 A（推荐）：按时间桶归一后再判唯一

```python
# 原实现
starts = {item["start_text"] for item in group_items if item.get("start_text")}
group_start = starts.pop() if len(starts) == 1 else None

# 改为：截断到 10 分钟粒度，容忍抓取/记录抖动
buckets = {(i["start_text"] or "")[:15] for i in group_items if i.get("start_text")}
if len(buckets) == 1:
    group_start = max(i["start_text"] for i in group_items if i.get("start_text"))
else:
    group_start = None
```

`"01:20"` 与 `"01:21"` 归入同一桶 `"2026-09-25 01:2"` → 上提成功 → 121 张卡恢复 2 行。取 `max()` 可保证组头显示的是组内**最晚**时间，不会比实际更早。

**优点**：保留原优化收益，改动小。阈值可按需调整（`[:15]` 为 10 分钟，`[:14]` 为 1 小时）。

#### 方案 B：组内不唯一时显示区间

```python
if len(starts) > 1:
    group_start = f"{min(starts)} ~ {max(starts)}"
```

卡片内一律不渲染"折扣开始"，布局绝对整齐。

**优点**：布局 100% 一致，且用户能看到完整时间范围。  
**缺点**：组头文案偏长，手机上可能换行。

#### 方案 C：前端兜底

`app.js` 中改为「组内时间不唯一时也上提到组头」，卡片内永不渲染该行。**需改前端，且逻辑分散在两处，不如 A/B 集中于 `report.py`**。

**建议**：采用 **方案 A**。

---

## 四、问题 3（P1）：金额尾零被砍

### 4.1 现象

线上 Borderlands 卡片现价显示 **`¥12.7`**，应为 `¥12.70`。

### 4.2 根因

`src/report.py::format_amount`：

```python
text = f"{amount_int / 100:,.2f}".rstrip("0").rstrip(".")
```

实测各金额输出：

| 分值    | 应为      | 实际显示       |
| ----- | ------- | ---------- |
| 1270  | ¥12.70  | **¥12.7**  |
| 1200  | ¥12.00  | **¥12**    |
| 12050 | ¥120.50 | **¥120.5** |
| 100   | ¥1.00   | **¥1**     |
| 12700 | ¥127.00 | ¥127       |

同时影响比价栏：`₹199 ≈ ¥13.95` 恰好未触发，但若换成 `¥7.80` 会显示成 `¥7.8`。

### 4.3 解决方案

#### 方案 A：固定两位小数

```python
text = f"{amount_int / 100:,.2f}"
```

`1270 → ¥12.70`、`12700 → ¥127.00`。**语义最规范**，但原价会显示 `¥127.00`，略显冗长。

#### 方案 B（推荐）：按分值是否为 0 决定

```python
if amount_int % 100 == 0:
    text = f"{amount_int // 100:,}"          # 12700 → 127
else:
    text = f"{amount_int / 100:,.2f}"        # 1270  → 12.70
```

`1270 → ¥12.70`、`1200 → ¥12`、`12700 → ¥127`，兼顾规范与简洁。

#### 方案 C：按币种区分

日元/韩元等无小数币种取整，CNY/USD 保留两位。**当前只用 CNY，暂不需要**。

**建议**：采用 **方案 B**。

---

## 五、Action 运行状况（P2，正常）

### 5.1 排队时长

| 项目       | 数值                       |
| -------- | ------------------------ |
| 计划时刻     | CST 07:24（`24 23 * * *`） |
| 实际启动     | CST 07:52                |
| **排队延迟** | **28 分钟**                |

历史最坏记录（源码注释自述）：`0 19 * * *`（CST 03:00，美区下午峰顶）曾排队 **2h48m**，已改至 05:14 规避。

### 5.2 耗时分布

| Run | 触发        | 耗时      | 说明                            |
| --- | --------- | ------- | ----------------------------- |
| #8  | Scheduled | 9m 24s  |                               |
| #7  | Scheduled | 11m 19s |                               |
| #6  | Scheduled | 3m 42s  |                               |
| #5  | Manual    | 12m 17s |                               |
| #4  | Scheduled | **1s**  | 补跑被 `daily-guard` skip，**正常** |
| #3  | Scheduled | 12m 13s |                               |
| #2  | Scheduled | 5m 2s   |                               |
| #1  | Manual    | 4m 43s  |                               |

### 5.3 发现：补跑机制实际未生效

`daily-guard` 用 **12 小时窗口**判断"过去是否已有成功运行"，而主跑（05:14）与补跑（07:24）仅隔 **2h10m**。因此**只要主跑成功，补跑必然被 skip**。

8 次运行中补跑只真正执行过 1 次（#8，因主跑未在窗口内成功）。这与 README 中"靠补跑兜底主跑被丢弃的场次"一致，但注释里"能省一整轮抓取"的表述会让人误以为补跑常在跑。

**建议**：若确实需要兜底，把 guard 窗口收窄至 3 小时；若主跑已足够稳定，可考虑砍掉补跑以简化流程。

### 5.4 预抓吞吐

`prefetch` 预算 300，实测成功率：

| 日期         | 目标  | 中文名新取 | 成功率  |
| ---------- | --- | ----- | ---- |
| 2026-09-23 | 300 | 300   | 100% |
| 2026-09-23 | 300 | 299   | 100% |
| 2026-09-24 | 300 | 298   | 99%  |

限流配置 `steam_rate_limit: "150 / 300s"` + `steam_min_interval: 2.0` —— 两者各给出 30 次/分钟上限，实际 ~~20~~30 次/分钟，300 次需 ~15 分钟。`waited_seconds` 达 484s，说明存在额外退避。

> 注：这三个 100% 的成功率**不代表没问题** —— 它统计的是"请求成功且拿到 name"，而 bug 场景下 `info()` 返回 `None` 时 `title_zh_fetched` 不计数。真实的"本该拿到却被丢弃"的数量，需修复后才能准确统计。

---

## 六、修复优先级建议

| 优先级    | 问题                  | 文件                             | 改动量  |
| ------ | ------------------- | ------------------------------ | ---- |
| **P0** | 中文名被 appid 重定向吞掉    | `src/steam.py::info`           | ~3 行 |
| **P1** | 详情区多一行（组 start 不唯一） | `src/report.py::build_groups`  | ~5 行 |
| **P1** | 金额尾零被砍              | `src/report.py::format_amount` | ~4 行 |
| P2     | 补跑 guard 窗口与间隔矛盾    | `.github/workflows/daily.yml`  | 决策项  |

### 建议的修复顺序

1. **先修 P0**（`steam.py::info`），这是用户直接感知最大的问题。
2. 跑一次 `prefetch` 或写一次性脚本，回填 2368 条缺失的中文名。
3. **再修 P1 两项**（同文件 `report.py`，可一次提交）。
4. P2 需产品决策，单独处理。

### 补充建议：补充单元测试

三个 bug 都属"静默失败"，现有测试未能拦截。建议补：

- `test_steam_info_handles_redirect`：mock 返回 key 与请求 appid 不同的响应，断言能取到 name。
- `test_build_groups_tolerates_minute_jitter`：组内 `01:20` + `01:21`，断言 `group_start` 非 `None`。
- `test_format_amount_keeps_two_decimals`：`1270 → "¥12.70"`、`12700 → "¥127"`。

---

## 七、附：本次检查的证据来源与局限

**证据来源**：

- 线上 `index.html`（3431 字节）与 `data.js`（77KB，经 allorigins 代理获取）
- `data` 分支 `state.json`（4.5MB，全量 `game_meta` 与 `run_log`）
- GitHub Actions 运行记录（`run #8` 页面、workflow 列表）
- Steam `appdetails` 接口实测（本次对 17 个 appid 发起请求）

**局限**：

- 线上 `static/app.js` / `static/app.css` **未能下载成功**（allorigins 对这两个路径持续返回 520/522）。但已通过 `data.js` 的事实 + `main` 分支源码 + commit 一致性（`3b72077` = main 最新）完成交叉验证，结论不受影响。
- 沙箱环境无法直连 `mumeione.github.io` 与 `api.github.com`（TLS 被中间人拦截），部分数据经第三方代理获取。

**注**：仓库 `src/httpclient.py::system_ca_bundle()` 正是为解决同类 TLS 中间人问题而写（从系统证书库导出 PEM），本次检查环境的网络限制与该函数处理的问题同源。
