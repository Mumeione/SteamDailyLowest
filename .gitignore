config.json
__pycache__/
*.pyc
.venv/
venv/

# data/ 整体不入库（probe 产物、logs、临时状态、本地 state.json）。
# 状态库由独立的 data 分支持有（见 .scratch/deploy/spec.md）——
# main 不留 state.json 历史；旧写法 `data/* + !data/state.json` 只在
# 「状态留 main」的方案里才需要（DEVELOPMENT.md §8 坑 4）。
data/

output/

# 双 agent 的工作记忆与历史探针归档，不入库（探针结论已沉淀进 docs/DEVELOPMENT.md）
.workbuddy/
tools/probes/
